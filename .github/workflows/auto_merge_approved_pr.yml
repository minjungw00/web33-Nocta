name: "Auto Merge Approved PRs"

on:
  pull_request_review:
    types: [submitted]

jobs:
  auto_merge:
    runs-on: ubuntu-latest
    steps:
      - name: "Get Pull Request"
        id: pr
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          result-encoding: string
          script: |
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
            });
            core.setOutput('prData', JSON.stringify(pr));

      - name: "Check Approvals"
        id: check
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const pr = JSON.parse(core.getInput('prData'));
            const reviews = await github.rest.pulls.listReviews({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.number,
            });
            const approvals = reviews.data.filter(review => review.state === 'APPROVED');
            core.setOutput('approved', approvals.length >= 1);

      - name: "Merge PR"
        if: steps.check.outputs.approved
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            await github.rest.pulls.merge({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
            });
